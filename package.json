{
  "name": "@f/form-controls",
  "description": "Retrieve a list of controls from a form",
  "repository": "git://github.com/micro-js/form-controls.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/form-elements": "^1.0.0",
    "@f/slice": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
